package com.minecraft.core.database.enums;

public enum Columns {
    UUID,
    NAME,
    CREATED_AT,
    LAST_SEEN,
    WINS,
    LOSSES,
    KILLS,
    DEATHS,
    BEDS_BROKEN,
    LEVEL,
    EXPERIENCE
}
